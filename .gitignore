# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/

# Venvs / envs
.venv/
env_*/
venv/

# Editor / Kit / OS cruft
imgui.ini
.DS_Store
*.swp

# Build / artifact scratch (data lives outside the repo, under the data root)
build/
dist/
*.log


# W&B run cache (anywhere in the tree) + secrets that must never be committed
wandb/
.netrc
*.key

# Training run outputs (checkpoints, tfevents): too large for git, live under the data root
experiments/*/logs/
archive/*/logs/
nohup.out

# platform-era content, untracked 2026-08-31: repo is paper/doc + part1 + venv recipe
archive/
tools/
isaacsim-editor
